# AIOps Dashboard

Комплексная система мониторинга и прогнозирования нагрузки серверов с использованием машинного обучения для временных рядов.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Технологический стек](#технологический-стек)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Запуск](#запуск)
- [API Документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Разработка](#разработка)
- [Тестирование](#тестирование)
- [Деплой](#деплой)
- [Troubleshooting](#troubleshooting)

---

## Описание

AIOps Dashboard - это полнофункциональная система для мониторинга метрик серверов, прогнозирования нагрузки и обнаружения аномалий. Система состоит из:

- **Backend API** (FastAPI) - REST API для работы с данными и прогнозирования
- **Frontend Dashboard** (Streamlit) - интерактивный веб-интерфейс для визуализации
- **Forecasting Engine** (Prophet) - модуль прогнозирования временных рядов
- **Database** (PostgreSQL) - хранение метрик и предсказаний

## Возможности

### 📊 Мониторинг
- Отслеживание метрик серверов в реальном времени
- Визуализация исторических данных
- Анализ нагрузки по серверам и метрикам
- Обнаружение аномалий

### 🔮 Прогнозирование
- Прогнозирование нагрузки на основе Prophet
- Доверительные интервалы
- Поддержка различных метрик (CPU, память, диск, сеть)
- Автоматическое обучение моделей

### 📈 Аналитика
- Сравнительный анализ серверов
- Корреляционный анализ метрик
- Heatmap распределения нагрузки
- Статистические отчеты

### 🚨 Алертинг
- Настраиваемые правила алертов
- Определение перегрузки и простоя серверов
- Классификация по уровням критичности

---

## Технологический стек

### Backend
- **FastAPI** 0.104.1 - современный веб-фреймворк
- **SQLAlchemy** 2.0.23 - ORM для работы с БД
- **PostgreSQL** 16.9 - реляционная БД
- **Prophet** 1.1.5 - прогнозирование временных рядов
- **Pydantic** 2.5.0 - валидация данных

### Frontend
- **Streamlit** 1.29.0 - фреймворк для дашбордов
- **Plotly** 5.18.0 - интерактивная визуализация
- **Pandas** 2.1.4 - обработка данных

### Infrastructure
- **Docker** & **Docker Compose** - контейнеризация
- **Apache HTTPd** 2.4 - reverse proxy
- **Keycloak** 26.4.6 - аутентификация (опционально)

---

## Требования

- **Python** 3.12+
- **PostgreSQL** 16.9+ (или Docker)
- **Docker** & **Docker Compose** (для контейнеризации)
- **Git** (для клонирования репозитория)

---

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd dashboard
```

### 2. Установка зависимостей

#### Backend

```bash
cd src/app
pip install -r requirements.txt
```

#### Frontend

```bash
cd src/ui
pip install -r requirements.txt
```

#### Для разработки (тесты)

```bash
pip install -r tests/requirements.txt
```

### 3. Настройка базы данных

#### Вариант A: Использование Docker (рекомендуется)

```bash
cd docker/postgres
docker-compose up -d
```

#### Вариант B: Локальная установка PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE server_metrics;
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=server_metrics

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Keycloak (опционально)
KEYCLOAK_URL=http://localhost:8087/keycloak
KEYCLOAK_REDIRECT_URI=http://localhost:8501
KEYCLOAK_CLIENT_SECRET=clientsecret
```

---

## Конфигурация

### База данных

Настройки подключения к БД находятся в `src/app/connection.py` и могут быть переопределены через переменные окружения.

### API

API настраивается в `src/app/main.py`. По умолчанию запускается на `http://localhost:8000`.

### UI

UI настраивается в `src/ui/main.py`. По умолчанию запускается на `http://localhost:8501`.

---

## Запуск

### Локальный запуск (без Docker)

#### 1. Запуск Backend API

```bash
cd src/app
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

API будет доступен по адресу: `http://localhost:8000`
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

#### 2. Запуск Frontend Dashboard

```bash
cd src/ui
streamlit run main.py --server.port 8501
```

Dashboard будет доступен по адресу: `http://localhost:8501`

### Запуск с Docker

#### Запуск всех сервисов

```bash
cd docker/all
docker-compose up -d
```

#### Запуск отдельных сервисов

**Backend API:**
```bash
cd docker/app
docker-compose up -d
```

**Frontend UI:**
```bash
cd docker/ui
docker-compose up -d
```

**PostgreSQL:**
```bash
cd docker/postgres
docker-compose up -d
```

### Остановка сервисов

```bash
cd docker/all
docker-compose down
```

---

## API Документация

### Базовый URL

```
http://localhost:8000/api/v1
```

### Основные эндпоинты

#### Database Operations
- `GET /vms` - список всех виртуальных машин
- `GET /vms/{vm}/metrics` - метрики для VM
- `GET /stats` - статистика базы данных
- `POST /cleanup` - очистка старых данных

#### Facts (Фактические метрики)
- `GET /facts` - получение исторических метрик
- `GET /facts/latest` - последние метрики
- `POST /facts` - создание метрики
- `POST /facts/batch` - пакетное создание
- `GET /facts/statistics` - статистика по метрикам

#### Predictions (Предсказания)
- `GET /predictions` - получение предсказаний
- `GET /predictions/future` - будущие предсказания
- `POST /predictions` - сохранение предсказания
- `POST /predictions/batch` - пакетное сохранение
- `GET /predictions/compare` - сравнение факт vs прогноз

### Полная документация

Интерактивная документация доступна по адресам:
- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

Подробная документация API: [API_ENDPOINTS.md](./API_ENDPOINTS.md)

### Примеры использования

#### Получение метрик

```bash
curl "http://localhost:8000/api/v1/facts?vm=DataLake-DBN1&metric=cpu.usage.average&days=7"
```

#### Создание метрики

```bash
curl -X POST "http://localhost:8000/api/v1/facts" \
  -H "Content-Type: application/json" \
  -d '{
    "vm": "DataLake-DBN1",
    "timestamp": "2025-01-27T12:00:00",
    "metric": "cpu.usage.average",
    "value": 45.5
  }'
```

#### Получение предсказаний

```bash
curl "http://localhost:8000/api/v1/predictions?vm=DataLake-DBN1&metric=cpu.usage.average"
```

---

## Структура проекта

```
dashboard/
├── src/
│   ├── app/              # Backend API (FastAPI)
│   │   ├── api/         # API endpoints
│   │   ├── models.py    # Database models
│   │   ├── schemas.py   # Pydantic schemas
│   │   ├── dbcrud.py    # Database CRUD operations
│   │   ├── facts_crud.py # Facts CRUD
│   │   ├── preds_crud.py # Predictions CRUD
│   │   └── main.py      # FastAPI application
│   │
│   └── ui/              # Frontend Dashboard (Streamlit)
│       ├── pages/       # Page components
│       ├── components/  # UI components
│       └── utils/       # Utilities
│
├── forecast/            # Forecasting module
│   ├── forecaster.py   # Main forecaster
│   ├── model_training.py
│   ├── model_tuning.py
│   └── ...
│
├── docker/             # Docker configurations
│   ├── app/           # API container
│   ├── ui/            # UI container
│   ├── postgres/      # Database container
│   └── httpd/         # Reverse proxy
│
├── tests/              # Test suite
│   ├── test_dbcrud.py
│   ├── test_factscrud.py
│   ├── test_predscrud.py
│   └── test_api_endpoints.py
│
├── notebooks/          # Jupyter notebooks
├── data/              # Data files
│   ├── source/       # Raw data
│   └── processed/    # Processed data
│
├── ARCHITECTURE.md    # Architecture documentation
├── API_ENDPOINTS.md   # API documentation
├── TESTING.md         # Testing guide
└── README.md          # This file
```

Подробная архитектура: [ARCHITECTURE.md](./ARCHITECTURE.md)

---

## Разработка

### Настройка окружения разработки

1. Клонируйте репозиторий
2. Создайте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:

```bash
pip install -r src/app/requirements.txt
pip install -r src/ui/requirements.txt
pip install -r tests/requirements.txt
```

### Структура кода

- **Backend**: `src/app/`
- **Frontend**: `src/ui/`
- **Forecasting**: `forecast/`
- **Tests**: `tests/`

### Стиль кода

Рекомендуется использовать:
- **black** для форматирования
- **flake8** для линтинга
- **mypy** для проверки типов

```bash
pip install black flake8 mypy
black src/
flake8 src/
mypy src/
```

---

## Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src/app --cov-report=html

# Используя скрипт
./run_tests.sh --coverage
```

### Типы тестов

- **Unit tests** - тестирование отдельных функций/классов
- **Integration tests** - тестирование API endpoints
- **Coverage** - отчет о покрытии кода тестами

Подробная документация: [TESTING.md](./TESTING.md)

### Примеры

```bash
# Запуск конкретного теста
pytest tests/test_dbcrud.py::TestDBCRUD::test_get_all_vms

# Запуск только unit тестов
pytest -m unit

# Запуск только integration тестов
pytest -m integration
```

---

## Деплой

### Docker Compose

Полный стек с Docker Compose:

```bash
cd docker/all
docker-compose up -d
```

### Production настройки

1. **Обновите CORS** в `src/app/main.py`:
```python
allow_origins=["https://yourdomain.com"]  # Вместо ["*"]
```

2. **Настройте SSL** через Apache HTTPd
3. **Используйте переменные окружения** для секретов
4. **Настройте мониторинг** и логирование

### Переменные окружения для production

```env
# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=your_user
DB_PASSWORD=secure_password
DB_NAME=server_metrics

# Security
KEYCLOAK_URL=https://yourdomain.com/keycloak
KEYCLOAK_CLIENT_SECRET=secure_secret

# API
API_HOST=0.0.0.0
API_PORT=8000
```

---

## Загрузка данных

### Через API

```python
import requests

# Создание метрики
response = requests.post(
    "http://localhost:8000/api/v1/facts",
    json={
        "vm": "DataLake-DBN1",
        "timestamp": "2025-01-27T12:00:00",
        "metric": "cpu.usage.average",
        "value": 45.5
    }
)
```

### Через утилиты

Используйте `utils/data_loader.py` для загрузки данных из Excel/CSV файлов.

---

## Troubleshooting

### Проблема: База данных недоступна

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте переменные окружения
3. Проверьте подключение: `psql -h localhost -U postgres -d server_metrics`

### Проблема: API не отвечает

**Решение:**
1. Проверьте логи: `docker logs dashboard`
2. Проверьте порт: `netstat -an | grep 8000`
3. Проверьте конфигурацию в `.env`

### Проблема: UI не загружает данные

**Решение:**
1. Проверьте подключение к API
2. Проверьте CORS настройки
3. Проверьте логи Streamlit
4. Убедитесь, что данные есть в базе

### Проблема: Тесты не проходят

**Решение:**
1. Убедитесь, что установлены все зависимости
2. Проверьте, что тестовая БД доступна
3. Запустите с `-v` для подробного вывода: `pytest -v`

---

## Дополнительная документация

- [Архитектура системы](./ARCHITECTURE.md) - подробное описание архитектуры
- [API Документация](./API_ENDPOINTS.md) - полное описание API endpoints
- [Руководство по тестированию](./TESTING.md) - как писать и запускать тесты
- [Код ревью](./CODE_REVIEW.md) - анализ кодовой базы

---

## Основные компоненты

### Backend API (`src/app/`)

- **DBCRUD** - общие операции с БД
- **FactsCRUD** - работа с фактическими метриками
- **PredsCRUD** - работа с предсказаниями
- **API Endpoints** - REST API endpoints

### Frontend UI (`src/ui/`)

- **Fact Page** - отображение фактических данных
- **Forecast Page** - прогнозирование
- **Analysis Page** - аналитика и сравнение

### Forecasting (`forecast/`)

- **ProphetForecaster** - основной класс прогнозирования
- **Model Training** - обучение моделей
- **Model Tuning** - подбор гиперпараметров
- **Model Storage** - сохранение моделей

---

## Примеры использования

### Загрузка данных в базу

```python
from src.app.connection import SessionLocal
from src.app.facts_crud import FactsCRUD
from src.app.schemas import MetricFactCreate
from datetime import datetime

db = SessionLocal()
crud = FactsCRUD(db)

metric = MetricFactCreate(
    vm="DataLake-DBN1",
    timestamp=datetime.now(),
    metric="cpu.usage.average",
    value=45.5
)

crud.create_metric_fact(metric)
```

### Генерация прогноза

```python
from forecast.forecaster import ProphetForecaster
from src.app.connection import SessionLocal
from src.app.dbcrud import DBCRUD

db = SessionLocal()
crud = DBCRUD(db)
forecaster = ProphetForecaster()

result = forecaster.generate_forecast(
    db=db,
    crud=crud,
    vm="DataLake-DBN1",
    metric="cpu.usage.average",
    periods=48,
    freq="30min"
)
```

---

## Roadmap

- [ ] Полная интеграция Keycloak для аутентификации
- [ ] Добавление WebSocket для real-time обновлений
- [ ] Расширенная система алертов с уведомлениями
- [ ] Поддержка дополнительных алгоритмов прогнозирования
- [ ] Экспорт отчетов в PDF/Excel
- [ ] Интеграция с системами мониторинга (Prometheus, Grafana)

---

## Contributing

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Guidelines

- Следуйте стилю кода проекта
- Добавляйте тесты для нового функционала
- Обновляйте документацию
- Пишите понятные commit messages

---

## License

[Укажите лицензию проекта]

---

## Контакты

[Укажите контактную информацию]

---

## Благодарности

- Facebook Prophet для прогнозирования временных рядов
- FastAPI за отличный фреймворк
- Streamlit за простоту создания дашбордов

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025-01-27

